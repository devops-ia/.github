name: Auto-assign Issue

on:
  workflow_call:
    inputs:
      teams:
        description: Comma-separated list of teams to assign
        required: true
        type: string
      remove_previous_assignees:
        description: Remove previously assigned users/teams before assigning
        required: false
        type: boolean
        default: true
      num_of_assignee:
        description: Number of assignees to pick per team
        required: false
        type: number
        default: 0
    secrets:
      PAT_GITHUB:
        required: true

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Auto-assign issue
        uses: pozil/auto-assign-issue@07fe6dc0e9771842b428f5739098d6140734e226 # v4.0.0
        with:
          repo-token: ${{ secrets.PAT_GITHUB }}
          teams: ${{ inputs.teams }}
          removePreviousAssignees: ${{ inputs.remove_previous_assignees }}
          numOfAssignee: ${{ inputs.num_of_assignee }}
